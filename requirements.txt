# HINT Study Console -- Version 1.5.0 (participant-controlled activity start)

PySide6>=6.7
PyYAML>=6.0
numpy>=1.26
pyserial>=3.5

# Real joystick/gamepad enumeration + polling (SDL)
pygame>=2.6.1

# Real microphone enumeration + input-stream verification (PortAudio)
sounddevice>=0.5.1

# Offline command-level speech-to-text; audio remains on the study PC
vosk==0.3.45

pytest>=8.0

# HoloLens 2 / HL2SS desktop client dependencies.
# The official HL2SS Python modules themselves are selected from the downloaded
# HL2SS repository's viewer folder in the Devices page.
opencv-python>=4.10
av>=13.0
requests>=2.32

# Persistent Windows Console <-> Ubuntu continuous-navigation worker transport
websockets>=13.0
