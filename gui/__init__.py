"""Qt GUI layer. Pages depend on core.application_controller.ApplicationController
and communicate with it only through manager method calls and the event bus
signal -- never the other way around."""
